<h1 class="text-center">Books for sale by <%= @seller.name %> </h1>

<div class="container">
  <div class="row">
    <% @books.each do |book| %>
    <div class="d-flex flex-column col-4">
    <%=  book_card(book)  %>
    </div>
    <% end %>
  </div>

  <%= paginate @books %>
</div>