<div class="container">
  <div class="row">
     <% @books.each do |book| %>
     <div class="d-flex flex-column col-4">
     <%=  book_card(book)  %>
     </div>
    <% end %>
  </div>

  <%= paginate @books %>
</div>